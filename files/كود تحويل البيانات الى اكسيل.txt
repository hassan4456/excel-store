javascript:(async function(){    const s = '.paginate_button.next:not(.disabled)';    let d = [];    function f1(){      const t = document.querySelector('table');      if(!t) return;      const n = t.querySelectorAll('tr');      if(d.length === 0){        const tHead = Array.from(n[0].querySelectorAll('th')).map(t=>t.innerText.trim());        d.push(tHead);      }      for(let i=1;i<n.length;i++){        const row = Array.from(n[i].querySelectorAll('td')).map(td=>td.innerText.trim());        if(row.length > 0) d.push(row);      }    }    function f2(data){      const filename = prompt("ÇßÊÈ ÇÓã ÇáãáÝ:", "student_data");      if(!filename) return;      const e = '\uFEFF';      let n = data.map(row => row.map(cell => `"${String(cell).split('"').join('""')}"`).join(';')).join('\n');      const o = new Blob([e+n], {type:'text/csv;charset=utf-8;'});      const r = document.createElement('a'), a = URL.createObjectURL(o);      r.setAttribute('href', a);      r.setAttribute('download', filename + '.csv');      r.style.visibility = 'hidden';      document.body.appendChild(r);      r.click();      document.body.removeChild(r);    }    async function f3(){      for(;;){        f1();        const t = document.querySelector(s);        if(t){          t.click();          await new Promise(resolve => setTimeout(resolve, 2000));        } else break;      }      f2(d);    }    f3();  })();